version: '3.1'
services:

   mongo:
    image: mongo
    restart: always   
    ports:
      - 27017:27017

   hub-service:   
    build: .
    restart: always
    ports:
      - 5000:80
    environment:
      MongoDBConfig__Host: mongo 
    depends_on:
      - mongo
